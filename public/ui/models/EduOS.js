/* ui/models/EduOS.js — no pre-fetch; each tab fetches lazily via renderEduOSTab(). */
window.JM = window.JM || {}; JM.Models = JM.Models || {};
JM.Models.EduOS = {
  fetch: async function () { return {}; }
};
