/* ui/models/LangPicker.js — no pre-fetch; language list is in window.JM_LANGS. */
window.JM = window.JM || {}; JM.Models = JM.Models || {};
JM.Models.LangPicker = {
  fetch: async function () { return {}; }
};
